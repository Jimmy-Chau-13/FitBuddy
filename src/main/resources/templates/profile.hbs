{{#partial "title"}} YOUR PROFILE{{/partial}}


{{#partial "content"}}
    <body>
    <div id="topMenu">
        <h1> Welcome {{username}}! </h1>

    </div>

    <div id="optionDiv">

        <div class="btn-group">
            <button type="button" class="btn btn-primary" id="addBtn">Add Workout</button>
            <button type="button" class="btn btn-primary" id="findBtn">Find Workout</button>
        </div>
        <div id="logoutBtn">
            <form action ="/logout" method = "post">
                <input type="submit" value="Logout" id="logoutBtn2" class="indexButton" />
             </form>
        </div>
    </div>

    <div id="calendar"></div>

    {{> addModal.hbs}}
    {{> viewModal.hbs}}
    {{> editModal.hbs}}
    {{> graphModal.hbs}}
    </body>
{{/partial}}
{{> base.hbs}}

<script type="text/javascript">
    $(document).ready(function (response) {

//////////////////////////////////// ALL FULL CALENDAR FUNCTIONALITY////////////////////////////////////////////////
        $("#calendar").fullCalendar({

            events: [
                {{{eventArray}}}
            ],

            dayClick: function(date) {
                date = date.format();  //yyyy-MM-dd
                console.log(date);
                date = formatDate(date);
                openViewModal(date);
            }
        });
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
    });
</script>